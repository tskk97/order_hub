import time


def send_order_email(order_id):
    # Simulate email sending with a delay
    time.sleep(5)  # Simulate a delay in sending the email
    print(f"Email for created order {order_id} has been sent.")
